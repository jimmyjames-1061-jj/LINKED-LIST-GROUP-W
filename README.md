The python program contains two classes:
1-CircularNode-represents each node in the list.
2-CircukarLinkedList-Manages the list operations.
HOW THE PROGRAM WORKS:
1-The `tail` node always points back to the `head`, forming a loop.
2.`print_list()` traverses from `head` until it loops back.
